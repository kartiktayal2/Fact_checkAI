# Fact_checkAI
Fact_CheckAI is a Flask-based NLP application that analyzes text claims and predicts whether they are likely factual or misleading. It uses text preprocessing, TF-IDF feature extraction, and a machine learning classifier. The app runs locally for development and demonstration.
